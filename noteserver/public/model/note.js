class Note {
    constructor(
        title,
        message,
        status,
        taskDate,
        priority
    ){
        this.title = title;
        this.message = message;
        this.status = status;
        this.taskDate = taskDate;
        this.priority = priority;
    }
}
export default {Note};